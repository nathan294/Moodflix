import 'package:flutter/material.dart';

class WishedMoviesList extends StatelessWidget {
  const WishedMoviesList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
